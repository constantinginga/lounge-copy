@page "/groups/{groupName}"
@page "/groups/{groupName}/{selectedPostId:long}"

<GroupPosts GroupName="@groupName" SelectedPostId="@selectedPostId"></GroupPosts>

@code {
    [Parameter]
    public string groupName { get; set; } = "lounge";
    [Parameter]
    public long? selectedPostId { get; set; }
}